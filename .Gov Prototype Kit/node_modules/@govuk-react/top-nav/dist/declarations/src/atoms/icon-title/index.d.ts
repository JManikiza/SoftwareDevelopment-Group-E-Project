import * as React from 'react';
declare const IconTitle: React.FC<IconTitleProps>;
export interface IconTitleProps {
    icon?: React.ReactNode;
    children?: React.ReactNode;
}
export default IconTitle;
